import React from 'react';
// Import components
import Weather from '../components/weather/Weather';

const Home = () => {
  return (
    <div>
      <Weather />
    </div>
  );
};

export default Home;
